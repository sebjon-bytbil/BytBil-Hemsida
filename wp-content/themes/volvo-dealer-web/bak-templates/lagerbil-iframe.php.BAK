<?php /* Template Name: Fordonsurval */ get_header(); ?>
	<?php
			$post_object = get_field('assortment-choice');
			if( $post_object ):
			$post = $post_object;
			setup_postdata( $post );
			?>
	<script type="text/javascript">
			var accesspack = "<?php the_field('accesspaket' , 'options');?>"; // name of dealercode in .NET package
			var iframewidth = "100%"; // iframewidth
			var criteriaString = "<?php the_field('assortment-string'); ?>";
			var showPage = "<?php the_field('assortment-type'); ?>"; // name of the page
			var scrollHeight = "100"; // height to scroll anchor from absolute top of page.
	</script>
	<script type="text/javascript" src="http://access.bytbil.com/<?php the_field('accesspaket' , 'options');?>/access/Content/GetContent/1/jquery-1.5.2.min.js"></script>
	<script type="text/javascript" src="http://access.bytbil.com/<?php the_field('accesspaket' , 'options');?>/access/Content/GetContent/1/jquery.ba-postmessage.js"></script>
	<script type="text/javascript" src="http://access.bytbil.com/<?php the_field('accesspaket' , 'options');?>/access/Content/GetContent/1/jquery.expandable-iframe-responsive.js"></script>
	<?php wp_reset_postdata();  ?>
<?php endif; ?>
	<div id="primary" class="content-area">
		<div id="content" class="site-content" role="main">
			<div class="wrapper">
				<article id="post-<?php the_ID(); ?>" <?php post_class(); ?>>
					<div class="left-column black-page">

						<header class="entry-header">
							<h1 class="entry-title"><?php echo empty( $post->post_parent ) ? get_the_title( $post->ID ) : get_the_title( $post->post_parent ); ?></h1>
						</header><!-- .entry-header -->

						<div class="side-menu-container">
							<ul class="side-menu-large">

								<?php	new_volvo_menu('bottom-buy'); ?>

							</ul>
							<!--<ul class="side-menu-small">
							<?php volvo_get_custom_menu('Bilmeny', 'side-menu-small'); ?>
							</ul>-->
							<ul class="side-menu-small">
								<?php new_volvo_menu('bilmeny'); ?>
							</ul>
						</div>

					</div>

					<div class="right-column iframe" style="text-align: left; height:100%; width: 100%;">
						<div class="ContentAP" id="iframeDiv"></div>
					</div><!-- .entry-content -->
				</article><!-- #post -->

			</div>


		</div><!-- #content -->
	</div><!-- #primary -->
	<script type="text/javascript">

	$(document).ready(function(e) {
		var pathname = window.location;

		$(".side-menu-large li").each(function(){
			var object = $(this).find("a");
			var url = object.attr("href");
			if(pathname==url){
				object.css('font-weight','bold');
			}
		});

		/*$(".side-menu-small li").each(function(){
			var object = $(this).find("a");
			var url = object.attr("href");
			if (url.indexOf("bygg-din-volvo") >= 0){
				object.attr("href",url+"?width=998&height=670");
				object.attr("data-lyte-options", "width:998 height:670 scrollbars:no autoResize:false");
				object.addClass('lytebox');
			}
		});*/

	});

	/**
 * Called to resize a given iframe.
 *
 * @param frame The iframe to resize.
 */
function resize( frame ) {
	var b = frame.contentWindow.document.body || frame.contentDocument.body,
		cHeight = $(b).height();

	if( frame.oHeight !== cHeight ) {
	$(frame).height( 0 );
	frame.style.height = 0;

	$(frame).height( cHeight );
	frame.style.height = cHeight + "px";

	frame.oHeight = cHeight;
	}

	// Call again to check whether the content height has changed.
	setTimeout( function() { resize( frame ); }, 250 );
}

/**
 * Resizes all the iframe objects on the current page. This is called when
 * the page is loaded. For some reason using jQuery to trigger on loading
 * the iframe does not work in Firefox 26.
 */
window.onload = function() {
	var frame,
		frames = document.getElementsByTagName( 'iframe' ),
		i = frames.length - 1;

	while( i >= 0 ) {
	frame = frames[i];
	frame.onload = resize( frame );

	i -= 1;
	}
};

</script>
<?php get_sidebar(); ?>
<?php get_footer(); ?>
